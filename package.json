{
  "name": "signal-q",
  "version": "2.1.0",
  "description": "Signal Q - Autonomous Transcendence Agent API",
  "main": "worker/index.js",
  "type": "module",
  "scripts": {
    "test": "./test-runner.sh",
    "test:vitest": "vitest run",
    "dev": "cd worker && npx wrangler dev index.js --remote --port 8787",
    "dev:fallback": "node scripts/wrangler-fallback.js dev",
    "deploy": "cd worker && npx wrangler deploy",
    "secret-scan": "node scripts/secret-scan.cjs",
    "secret-scan:install": "node scripts/secret-scan.cjs install",
    "validate": "npm run secret-scan && npm audit && npm run validate:openapi",
    "validate:openapi": "npx swagger-cli validate worker/src/openapi-core.json",
    "generate:openapi": "node scripts/gen-openapi-js.cjs",
    "build": "echo 'Validation build - checking worker syntax and OpenAPI spec' && npm run generate:openapi && cd worker && node --check index.js && echo '✅ Worker syntax is valid' && cd .. && npm run validate:openapi && echo '✅ OpenAPI specification is valid'"
  },
  "keywords": [
    "cloudflare-workers",
    "api",
    "transcendence-agent"
  ],
  "author": "quilross",
  "license": "MIT",
  "devDependencies": {
    "@apidevtools/swagger-cli": "^4.0.4",
    "@apidevtools/swagger-parser": "^12.0.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
